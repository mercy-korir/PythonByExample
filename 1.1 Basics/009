'''009  
Write a program that will ask for a number of days and then will 
show how many hours, minutes and seconds are in that number of days. '''

days=int(input('How many days has elapsed since the month began?'))
hours=days*24
minutes=hours*60
seconds=minutes*60
print(hours,'hours',minutes,'minutes',seconds,'seconds has elapsed since the month began')

